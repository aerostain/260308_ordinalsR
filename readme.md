# 260308

## Comandos usados en expss

* apply_labels
* read_spss # Es el mejor ya contiene add_labelled_class()
* read_sav + add_labelled_class()
* Son equivalentes `m$am %>% cro()` y `m %>% cross_cases(am)`

## Pendientes

* Entender el código de: `tab_last_sig_means`